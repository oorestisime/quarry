# @oorestisime/quarry

Compatibility wrapper for the package formerly published under this name.

The ClickHouse query builder is now published as [`quarry`](https://www.npmjs.com/package/quarry).
Existing `^0.8.0` consumers can update to `0.8.1` without changing imports, but
new projects should install and import `quarry` directly.
